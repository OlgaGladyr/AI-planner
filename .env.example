# Copy this file to `.env` for local use with `vercel dev` (optional).
# On the live site, you do NOT create this file — instead you add the same
# variable name in the Vercel dashboard (Project Settings → Environment Variables).
# Never commit a real key to GitHub.

ANTHROPIC_API_KEY=
